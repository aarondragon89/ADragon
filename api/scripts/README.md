# scripts

This folder is reserved for optional helper scripts (migrations, seed, diagnostics)
that mirror the structure used in BookCar and api-core projects.

Add project-specific scripts here when needed.
